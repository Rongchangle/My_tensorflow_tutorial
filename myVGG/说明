这个VGG是根据kevin28520的vgg代码, 按照自己风格重新写的vgg16神经网络(我自己写的第二个可以使用的神经网络)

思路: 网络的结构来源于VGG原论文, 小部分区域做了修改(kevin28520的修改).  

代码运行: 在cifar10上做训练和测试, 使用kevin28520链接的npy文件的参数做初始化(只是卷积核参数, 不包括全连接层的参数), 以0.01的学习率训练15000步, 
以0.001的学习率训练4000步. 最后只有78%的准确率. 和原作者kevin28520的85%的准确率差距很大. 甚至网络上随便一个教程都有85%的准确率. 暂时不知道原因.
不过这份代码可以给自己当做构建神经网络的模板格式使用.


可以当做模板的:
1. 从bin文件(数据集打包的)读取图片和标签的batch(csv文件也是差不多的,可以用这个代码参考,不过是换个reader而已)
2. 神经网络的卷积,池化,全连接如何组织构建.
3. 输出网上别人共享的.npy文件中参数的名称,大小. 把它们的部分或全部赋值到自己的神经网络.
4. 保存或读取.ckpt文件.
5. 训练,测试神经网络.
